<h1>My Favorite Bookmarks</h1>
 <% @likes.each do |like| %>
   <%= link_to like.bookmark.url, like.bookmark.url %>
   <% @object = LinkThumbnailer.generate(like.bookmark.url) %>
   <%= image_tag @object.images.first.src.to_s, class: 'img' %>
 <% end %>
