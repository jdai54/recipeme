<%= form_for [topic, bookmark] do |f| %>
  <div class="form-group">
    <%= f.text_field :url, class: 'form-control', placeholder: "Enter a bookmark url" %>
  </div>
  <div class="form-group">
    <%= f.submit "Save", class: 'btn btn-success' %>
  </div>
<% end %>
